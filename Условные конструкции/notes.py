'''
    Объединение логических выражений
    Логические операции
    and - если требуется, чтобы все логические выражения были истинны
    or  - если требуется, чтобы хотя бы одно лог. выражение было истинным
    not - изменение лог. значения на противоположное
'''

if x > 0:
    if y > 0:

if x > 0 and y > 0: